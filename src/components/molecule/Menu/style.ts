import styled from 'styled-components'
import { COLORS } from 'src/common'

export const List = styled.nav`
  display: flex;
  align-items: center;
  gap: 40px;
  a {
    color: ${COLORS.GRAY};
  }
`
